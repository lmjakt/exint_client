#ifndef CLUSTERPLOTTER_H
#define CLUSTERPLOTTER_H

#include <qpopupmenu.h>
#include <vector>
#include "../client.h"
#include "../meanPlotWindow/meanExpressionPlotter.h"

using namespace std;

class ClusterPlotter : public MeanExpressionPlotter
{
  Q_OBJECT
    
    public :
    ClusterPlotter(QWidget* parent=0, const char *name=0);
  
  void paintCluster(clusterSet* cs, int index);
  void paintClusterSet(clusterSet* cs);   // draw the centers only..
  
  protected :
    virtual void mousePressEvent(QMouseEvent* e);

  private slots :
    void loadGenes();
  void saveClusters();

  private :
    QPopupMenu* actionMenu;
  int clusterNumber;
  clusterSet* theCluster;

  signals :
    void pleaseLoadTheseGenes(vector<int>);
};

#endif
