#ifndef REGIONLISTITEM_H
#define REGIONLISTITEM_H

#include "../client.h"
#include <qlistview.h>

using namespace std;

classs RegionListItem : public QListViewItem
{

  public :
    RegionListItem(chromRegion* cr, int i, QListView* parent);
  
  int compare(QListViewItem* i, int col, bool ascending) const;
  int rtti() const {return rtid; }
  chromRegion* chRegion(){
    return(region);
  }

  private :
    chromRegion* region;
  int number;
  int rtid;
};

#endif
