#ifndef NEWSESSION_H
#define NEWSESSION_H

#include <qwidget.h>
#include <qstring.h>
#include <vector>
#include <qlineedit.h>
#include <qtextedit.h>

using namespace std;

class NewSession : public QWidget
{
  Q_OBJECT

    public :
    NewSession(QWidget* parent=0, const char* name=0);
  ~NewSession();

  private :
    QTextEdit descriptionArea;
  QLineEdit titleLine;
  QLineEdit keyWordLine;

  private slots :
    void commit();       // commit the changes..
  
  signals :
    void createSession(vector<QString>);
};

#endif

