#ifndef BLASTALIGNPAINTER_H
#define BLASTALIGNPAINTER_H

#include <qobject.h>
#include <qpainter.h>
#include <vector>
#include "probe_set/probe_set.h"

using namespace std;

class BlastAlignPainter : public QObject
{
  Q_OBJECT
    
    public :
    BlastAlignPainter();         // no need to do anything...
  void paintAlignments(vector<blastAlignment>* balls, QPainter* p, int xo, int yo, int w, int h);
  
 private:
  vector<blastAlignment> alignments;
};

#endif
