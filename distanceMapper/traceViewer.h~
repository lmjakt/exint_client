#ifndef TRACE_VIEWER
#define TRACE_VIEWER

#include <qwidget.h>
#include "../dataStructs.h"
#include <vector>

class TraceViewer : public QWidget
{
  Q_OBJECT
    public :
    TraceViewer(std::vector<tracePoint> pts, QWidget* parent=0, const char* name=0);

  ~TraceViewer();

  private slots:
    void showDrawing();
  
  signals :
    void deleteMe();

  private :
    TraceDrawer* drawer;
}
    

#endif
